import styled from 'styled-components';

import MainLayout from '$/containers/Layouts/Main';

export const Container = styled(MainLayout)``;

export const Ranking = styled.ul`
  padding-left: 0;
  margin: 0;
`;
